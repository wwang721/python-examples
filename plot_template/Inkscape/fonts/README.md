All `.ttf` font files were extracted from Apple’s macOS `.ttc` font collections using the `extract.py` script. They are therefore not separately licensed for redistribution.

These fonts generally cannot be installed system-wide on Windows and may produce a “not valid” error. However, they can still be used by applications such as Matplotlib or Inkscape by placing them in the application’s own font directory.
